import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mangelschots.dries.twitterproject',
  appName: 'twitterProject',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
